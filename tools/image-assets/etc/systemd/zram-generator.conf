# gaokun3: zram swap（内核 CONFIG_ZRAM=m + CONFIG_ZRAM_BACKEND_ZSTD=y）
# Live USB 场景下磁盘 swap 不可用，zram 是唯一 swap；
# 上限 4G，避免大内存设备压缩占用过高
[zram0]
zram-size = min(ram / 2, 4096)
# zstd 压缩率明显高于 lzo-rle，arm64 上解压开销可接受，适合内存紧张的平板形态
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
